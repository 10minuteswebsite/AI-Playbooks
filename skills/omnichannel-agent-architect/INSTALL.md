# Installation — Arquitecto Omnicanal v2

Canonical skill id:

`omnichannel-agent-architect`

Version:

`2.0.0`

## Codex

Install/replace the existing skill with this same directory.

Expected operational location may be product-managed or similar to:

`~/.codex/skills/omnichannel-agent-architect/`

Do not rename the skill unless all existing references have been audited.

## Claude Code

Install this exact same skill directory using Claude Code's Agent Skills-compatible mechanism.

Do not fork the content into a separate "Claude architect".

Keep `CLAUDE.md` as a thin bootstrap layer only.

## OpenCode

Install this exact same skill directory in a supported OpenCode/Agent Skills location.

Keep `AGENTS.md` and `opencode.json` in each repository.

Do not fork the skill into a separate "OpenCode architect".

## Candidate canonical GitHub source

Commit the candidate source under review to:

`10minuteswebsite/AI-Playbooks/skills/omnichannel-agent-architect/`

Until its introducing Pull Request receives human approval and is merged, that directory is the proposed canonical skill source. It becomes canonical after the human-approved merge; future changes then happen there first and operational installations are updated from that source.
